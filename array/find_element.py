# Write a program to check if an integer element is present in the list


def find_element(numbers, element):
    if type(numbers) == list:
        index = -1
        for num in numbers:
            index += 1
            if num == x:
                return index
        return -1
    else:
        return -1



